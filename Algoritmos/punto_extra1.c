#include <iso646.h>
#include <stdio.h>

int cantidad_agua(int arreglo[5], int tam);
int menor(int a, int b) {
    if(a>b) {
        return b;
    }
    if (a<b) {
        return a;
    }
    return a;
}

int main() {
    printf("Ingrese los valores enteros del arreglo");
    while ()
    return 0;

}

int cantidad_agua(const int arreglo[5]) {
    int agua = 0;
    int top_izq = 0;
    int top_der = 0;
    for (int i = 0; i < 5; i++) {
        top_izq
    }
}